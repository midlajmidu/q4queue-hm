# Q4Queue Project Rules

## Terminology Mapping
- Whenever the user uses the term "branch", they are referring to the `Organization` model in the database/code.
- Whenever the user uses the term "org", they are referring to the `ParentOrganization` model in the database/code.
